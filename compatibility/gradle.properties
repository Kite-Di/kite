# Defaults: the combination the project itself is built with. CI overrides each
# with -P to sweep the matrix.
agpVersion=9.2.1
kotlinVersion=2.3.21
kspVersion=2.3.9
kiteVersion=0.1.1

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
